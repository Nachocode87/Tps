let num1 = (parseInt(window.prompt('escriba un numero'))%2 ) ;

if (num1 == 0) {
    document.write('el numero si se es divisible por 2')
} else {
    document.write('el numero no se puede dividir en 2')
}